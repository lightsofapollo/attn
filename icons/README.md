# Icons

This directory contains app icons used for macOS bundling.

- `attn.icns` is currently a temporary placeholder icon.
- Regenerate the placeholder from source using:

```bash
scripts/generate-placeholder-icon.sh
```

When final brand assets are ready, replace both files:

- `attn-placeholder-source.png`
- `attn.icns`
