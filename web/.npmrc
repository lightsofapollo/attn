# engines in package.json only WARNS by default. The Node 22 floor is real —
# wrangler 4 refuses to run on less — so fail at install with a clear message
# instead of deep inside a build step (attn-6q7b.2).
engine-strict=true
