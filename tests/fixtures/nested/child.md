# Nested Document

This file lives in a subdirectory to test directory tree rendering and breadcrumb navigation.

## Purpose

- Verify sidebar shows nested directory structure
- Verify breadcrumb displays `nested/child.md`
- Verify navigation between files works

## Content

Some content to ensure the file renders properly in the viewer.

```bash
echo "Hello from nested/child.md"
```
